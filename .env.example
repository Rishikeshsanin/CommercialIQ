# Optional: enables live LLM augmentation for /api/agent.
# The deployed demo remains fully functional without this key.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# CommercialIQ App #4 — shared Supabase Project Hub.
# Use ONLY the public project URL + publishable key. Never place a secret/service-role key here.
SUPABASE_URL=https://nowlwprtcnieihelqjoa.supabase.co
SUPABASE_PUBLISHABLE_KEY=

# Optional browser-side values if a future client feature needs direct read access.
# CommercialIQ data remains isolated in schema `commercialiq` with SELECT-only RLS policies.
VITE_SUPABASE_URL=https://nowlwprtcnieihelqjoa.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=
