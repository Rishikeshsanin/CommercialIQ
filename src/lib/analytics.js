import { products, regions, segments, forecast, documents } from '../data/commercialData';
export const formatCurrency=(value,compact=false)=>compact?`₹${value.toFixed(2)}M`:new Intl.NumberFormat('en-IN',{style:'currency',currency:'INR',maximumFractionDigits:0}).format(value);
export const pct=value=>`${value>=0?'+':''}${value.toFixed(1)}%`;
export const totalSegmentCustomers=segments.reduce((sum,s)=>sum+s.size,0);
export function localAgentAnswer(question){const q=question.toLowerCase();const weakest=[...products].sort((a,b)=>a.growth-b.growth).slice(0,3);const strongest=[...products].sort((a,b)=>b.growth-a.growth).slice(0,3);const weakRegion=[...regions].sort((a,b)=>a.growth-b.growth)[0];if(q.includes('weak')||q.includes('declin')||q.includes('risk'))return{answer:`The clearest commercial risks are ${weakest[0].name} (${pct(weakest[0].growth)} growth) and ${weakest[1].name} (${pct(weakest[1].growth)}). ${weakRegion.name} is the weakest region at ${pct(weakRegion.growth)}. I would first validate product-mix and retention effects, then reduce excess inventory exposure on declining products while protecting high-value customers.`,tools:['get_product_performance','get_region_performance','detect_sales_anomalies'],sources:[documents[2],documents[0]],confidence:.91};if(q.includes('forecast')||q.includes('demand')||q.includes('inventory'))return{answer:`Demand is projected to rise from ${forecast[0].units.toLocaleString('en-IN')} units in ${forecast[0].month} to ${forecast[5].units.toLocaleString('en-IN')} by ${forecast[5].month}. Kinetra has the most immediate inventory pressure because its inventory is below forecast demand, while Solvexa carries excess inventory relative to a declining outlook. Use forecast intervals rather than a single point estimate before committing supply.`,tools:['forecast_sales','get_product_performance','calculate_inventory_gap'],sources:[documents[1]],confidence:.94};if(q.includes('customer')||q.includes('segment')||q.includes('retention'))return{answer:'Champions and Loyal Growth customers account for the strongest commercial value, while 742 customers are currently in the At Risk segment. Retention activity should prioritize high-value accounts with worsening recency and frequency before using broad discounting. The recommended next step is a ranked outreach list combining RFM score, value and recent trajectory.',tools:['get_customer_segments','identify_at_risk_customers','rank_next_best_actions'],sources:[documents[3],documents[0]],confidence:.92};if(q.includes('best')||q.includes('opportun')||q.includes('growth'))return{answer:`The strongest opportunities are ${strongest[0].name} (${pct(strongest[0].growth)}) and ${strongest[1].name} (${pct(strongest[1].growth)}). South is the leading region at ${pct(regions[0].growth)}. The practical action is to protect availability for growth products, prioritize high-value segments, and track whether incremental inventory translates into realized revenue rather than just higher stock.`,tools:['get_product_performance','get_region_performance','get_customer_segments'],sources:[documents[0]],confidence:.90};return{answer:'Commercial performance remains positive overall, but the portfolio is mixed. Revenue is growing, demand is forecast upward, and two products show strong momentum; at the same time, East-region softness and declining mature products need intervention. Ask me about demand forecasting, customer segments, inventory, regional performance, or commercial risks for a deeper tool-driven analysis.',tools:['get_revenue_summary','detect_sales_anomalies'],sources:[documents[0],documents[2]],confidence:.88};}
